name: azure_snowflake_dbt_local_data_platform
version: "1.0.0"
config-version: 2

profile: azure_snowflake_dbt_local_data_platform

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  azure_snowflake_dbt_local_data_platform:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
